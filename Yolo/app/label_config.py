from __future__ import annotations

CLASS_NAMES = ["Panel", "Machine", "Sheet"]
CLASS_NAME_TO_ID = {name: index for index, name in enumerate(CLASS_NAMES)}
CLASS_ID_TO_NAME = {index: name for index, name in enumerate(CLASS_NAMES)}
